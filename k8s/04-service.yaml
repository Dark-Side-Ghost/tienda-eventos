apiVersion: v1
kind: Service
metadata:
  name: tienda-eventos-service
spec:
  type: NodePort
  selector:
    app: tienda-eventos
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080

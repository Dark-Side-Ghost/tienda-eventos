apiVersion: v1
kind: Secret
metadata:
  name: tienda-eventos-secret
type: Opaque
stringData:
  # Ejemplo de dato sensible que en un caso real vendria de un
  # servicio de notificaciones/correo. Aqui es solo demostrativo,
  # no se usa dentro de la logica de negocio, pero muestra como
  # inyectar credenciales sin dejarlas en el codigo ni en la imagen.
  NOTIFICACIONES_API_KEY: "clave-demo-12345"
